[project]
name = "server"
version = "0.1.0"
description = "Enhanced MCP server for stock analysis with buying recommendations, top performers tracking, and price trajectory prediction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.8.1",
    "yfinance>=0.2.61",
    "pandas>=1.3.0",
    "numpy>=1.16.5",
    "schedule>=1.2.0",
    "pyyaml>=6.0",
]
